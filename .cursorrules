# AI 助手的项目全局规则

1. **演示文件位置**：每当用户要求创建、生成或编写“演示 (Demo)”文件、示例代码或演示时，这些文件的格式可能是html/vue等，您必须始终将这些文件保存并放置在 `Demos/` 目录中。
2. **目录创建**：如果 `Demos/` 文件夹不存在，请在保存文件之前创建它。
3. **保持根目录整洁**：除非用户明确要求，否则不要在根目录或其他目录中输出演示脚本或文件。
4. **样式快修规则**：当用户在指令中提及“样式快修”时，仅对页面代码（HTML/CSS/JS）进行直接、局部的修改和美化，不要执行任何自动拉起浏览器进行自动化验证、拨测、或模拟点击的操作。

### 项目脚本工具使用规范
1. 本项目所有的自动化脚本、排障脚本、更新工具 (如 `.py`, `.js`, `.sh` 等) 均统一存放于 `scripts/` 文件夹中。
2. AI 在后续分析代码、调用检查工具、或执行项目构建/更新操作时，请统一前往 `scripts/` 目录下寻找并调用对应的文件（例如 `python3 scripts/update.py`）。
